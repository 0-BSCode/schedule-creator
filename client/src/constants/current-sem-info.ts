import AcademicPeriodEnum from "@src/types/enums/academic-period-enum";

const currentSemInfo = Object.freeze({
  year: 2023,
  period: AcademicPeriodEnum.SECOND_SEMESTER,
});

export default currentSemInfo;
